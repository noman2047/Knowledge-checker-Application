import React from 'react';

const QuizDetails = ({getQuestion}) => {
  console.log(getQuestion);
  const {id,question,options,correctAnswer}=getQuestion;
  return (
    <div>
      
      
    </div>
  );
};

export default QuizDetails;